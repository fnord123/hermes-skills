# Third-party imports used by this skill's scripts (declared for tools/lint_skills.py).
# Both real deps are imported lazily (inside functions): fitz/PyMuPDF for PDF
# text/OCR work, matplotlib for the lab-trend chart — so the core pipeline runs
# on the stdlib alone.
PyMuPDF
matplotlib
